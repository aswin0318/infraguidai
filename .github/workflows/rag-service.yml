name: CI/CD - rag-service

on:
  push:
    branches: [main]
    paths:
      - "services/rag-service/**"
      - "shared/**"
      - ".github/workflows/rag-service.yml"
  pull_request:
    branches: [main]
    paths:
      - "services/rag-service/**"
      - "shared/**"
      - ".github/workflows/rag-service.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  id-token: write
  contents: write
  pull-requests: write

jobs:
  # ───────────── PR — sequential: lint → sca → trivy ─────────────
  lint:
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/_lint.yml
    with:
      service-path: services/rag-service
      runtime: python

  sca:
    needs: [lint]
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/_sca.yml
    with:
      service-name: rag-service
      service-path: services/rag-service
    secrets:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

  trivy:
    needs: [sca]
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/_docker-build.yml
    with:
      service-name: rag-service
      dockerfile: services/rag-service/Dockerfile
      context: .

  pr-check:
    name: PR Quality Gate
    needs: [trivy]
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - run: echo "All checks passed"

  # ───────────── Push to main — build, publish & deploy ─────────────
  prepare:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    outputs:
      sha: ${{ steps.sha.outputs.short }}
    steps:
      - id: sha
        run: echo "short=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

  publish:
    needs: [prepare]
    if: github.event_name == 'push'
    uses: ./.github/workflows/_docker-publish.yml
    with:
      image-name: infragui/rag-service
      tag: ${{ needs.prepare.outputs.sha }}
      dockerfile: services/rag-service/Dockerfile
      context: .

  cd:
    needs: [prepare, publish]
    if: needs.publish.result == 'success'
    uses: ./.github/workflows/_cd.yml
    with:
      chart: rag-service
      image-tag: ${{ needs.prepare.outputs.sha }}
